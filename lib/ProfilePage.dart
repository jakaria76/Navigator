import 'package:flutter/material.dart';

class ProfilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Profile'),
      ),
      body: Container(
        // Placeholder content, replace with your actual profile page content
        child: Center(
          child: Text('Your Profile Content'),
        ),
      ),
    );
  }
}
