
import 'package:flutter/material.dart';

class forgate extends StatelessWidget {
  const forgate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("forgate pasword page"),
      ),
    );
  }
}
